spring:
  application:
    name: mk-board-service
  datasource:
    url: jdbc:mysql://localhost:3306/mk_board?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: mysqluser
    password: mysqlpw
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
